'use strict';

var React = require('react-native');

var { StyleSheet } = React;

module.exports = StyleSheet.create({
    row: {
    	flex: 1, 
    	alignItems: 'center'
    },
    text: {
        fontSize: 15,
        color: "#000",
        marginBottom: 10
    }
});
